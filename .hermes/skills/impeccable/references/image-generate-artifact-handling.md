# Image-generate artifact handling

When generating multiple palettes, probes, or collage assets in one run:
- save each successful image into the project tree immediately
- do not leave critical outputs only in temporary cache paths
- use stable descriptive filenames per direction or role
- keep source crops and review artifacts separate from final production assets
- verify each file exists before asking for the next generation
